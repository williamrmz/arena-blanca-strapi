'use strict';

/**
 * servicio-encabezado router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::servicio-encabezado.servicio-encabezado');
