'use strict';

/**
 * servicio-encabezado service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::servicio-encabezado.servicio-encabezado');
