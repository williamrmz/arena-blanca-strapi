'use strict';

/**
 * servicio-experiencia router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::servicio-experiencia.servicio-experiencia');
