'use strict';

/**
 * servicio-experiencia controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::servicio-experiencia.servicio-experiencia');
