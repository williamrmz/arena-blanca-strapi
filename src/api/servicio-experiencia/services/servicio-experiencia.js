'use strict';

/**
 * servicio-experiencia service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::servicio-experiencia.servicio-experiencia');
