'use strict';

/**
 * servicio-galeria-habitacion service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::servicio-galeria-habitacion.servicio-galeria-habitacion');
