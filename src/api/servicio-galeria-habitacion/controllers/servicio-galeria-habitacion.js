'use strict';

/**
 * servicio-galeria-habitacion controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::servicio-galeria-habitacion.servicio-galeria-habitacion');
