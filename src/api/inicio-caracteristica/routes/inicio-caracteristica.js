'use strict';

/**
 * inicio-caracteristica router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::inicio-caracteristica.inicio-caracteristica');
